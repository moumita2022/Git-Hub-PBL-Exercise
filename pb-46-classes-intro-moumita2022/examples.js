/*

An example of a class with no constructor and no methods or properties

class Animal {
}

const dog = new Animal();

 */

/*

An example of a class with a constructor and a method "playNoise", and a property "noise"

class Animal {

    constructor(noise) {
        this.noise = noise;
    }

    playNoise() {
        console.log(this.noise);
    }
}

const dog = new Animal("woof!");

 */

